---
# Configuring Arvados

- name: Create config directory for Arvados
  ansible.builtin.file:
    path: /etc/arvados
    state: directory
    mode: '0755'

- name: Add Arvados config file
  copy:
      dest: "/etc/arvados/arvados.yml"
      content: |
        Clusters:
          bwarv:
